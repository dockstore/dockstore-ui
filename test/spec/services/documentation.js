'use strict';

describe('Service: DocumentationService', function () {

  // load the service's module
  beforeEach(module('dockstore.ui'));

  // instantiate service
  var DocumentationService;
  beforeEach(inject(function (_DocumentationService_) {
    DocumentationService = _DocumentationService_;
  }));

  // it('should do something', function () {
  //   expect(!!DocumentationService).toBe(true);
  // });

});
